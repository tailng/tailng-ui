{
  "name": "theme",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/tailng-ui/theme/src",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": ["scope:tailng", "type:theme"],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "cache": true,
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/dist/libs/tailng-ui/theme"],
      "options": {
        "project": "libs/tailng-ui/theme/ng-package.json",
        "tsConfig": "libs/tailng-ui/theme/tsconfig.lib.prod.json"
      }
    },
    "vite:test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "configFile": "libs/tailng-ui/theme/vitest.config.ts",
        "reportsDirectory": "coverage/libs/tailng-ui/theme",
        "watch": false
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    }
  }
}
